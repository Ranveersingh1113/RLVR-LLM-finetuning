# RLVR Math Project

The canonical project specification currently lives in `RLVR_MATH_PROJECT.md`.
This file is a lightweight pointer so the repository structure matches the
implementation plan while we build out the codebase.

